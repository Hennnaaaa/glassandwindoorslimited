// Real completed installation photos, curated by the business owner specifically
// for a "Completed Projects" showcase (residential and commercial glazing work).
export const completedProjects = Array.from({ length: 12 }, (_, i) => ({
  id: i + 1,
  src: `/images/completed-projects/project-${i + 1}.jpg`,
}));
